const greeting = (name) => {
  console.log(`Hello ${name} welcome to Node.js week`);
};

const hello = (name) => {
    console.log(`Hello ${name}`);
}

module.exports = {greeting, hello};
// module.exports = greeting

